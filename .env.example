# Supabase
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
NEXT_PUBLIC_SUPABASE_BUCKET=""
SUPABASE_SERVICE_ROLE_KEY=""

NEXT_PUBLIC_SITE_URL=""
NEXT_PUBLIC_APP_SCHEME="langquest-dev"

# App identity
TEAM_ID="apple_team_id_10_chars"
APP_IDS="com.etengenesis.langquest.development"
# Comma-separated SHA256 fingerprints in the same order as APP_IDS
# Get each via: eas credentials -p android (in logs after choosing app variant but before choosing selection)
ANDROID_SHA256_FINGERPRINTS=""

# Resend API credentials
RESEND_API_KEY="your_resend_api_key"
AUDIENCE_ID="your_resend_audience_id"

# Audio concatenation worker
AUDIO_CONCAT_WORKER_URL=""
AUDIO_CONCAT_WORKER_TOKEN=""